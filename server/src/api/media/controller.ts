import { Request, Response, NextFunction } from 'express';

export async function getAllMedia(
    req: Request,
    res: Response,
    next: NextFunction,
) {
    try {
    } catch (error) {}
}
